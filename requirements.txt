nfl_data_py>=0.3.2
pandas>=1.5
duckdb>=0.10
fastapi>=0.110
uvicorn>=0.29
# The draft socket's client (pipeline/draft_socket.py, imported by
# api/live.py at module scope). Present in a dev venv by accident -- something
# else pulls it in -- and absent from a clean install, where it took the whole
# API down at import with `ModuleNotFoundError: No module named 'websockets'`.
# Found by building the deployment image, which is the first clean install
# this project ever had.
websockets>=12.0
requests>=2.31
pytest>=8.0
httpx>=0.27
scipy>=1.11
playwright>=1.40
pypdf>=4.0
# Fernet, for pipeline/credentials.py -- the encrypted-at-rest store holding
# other people's ESPN account sessions. The first dependency this project has
# taken that is not a data source: the alternative was hand-rolling an AEAD on
# the one code path where a subtle mistake is unrecoverable for users who
# cannot be contacted (ESPN's fan API returns no email).
cryptography>=42.0
